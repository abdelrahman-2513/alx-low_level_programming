#include "main.h"

/**
 * _abs - if the number is negative it print out absolute
 * @n: the int to checked
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
