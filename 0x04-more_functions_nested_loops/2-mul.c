#include "main.h"
/**
 * mul - take two integers and return the multiplication of them
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the number of multiplication
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
